{application, erlagi, [
    {description, "Asterisk Gateway Interface (AGI) client for erlang/otp"},
    {vsn, tag},
    {registered, []},
    {applications, [kernel, stdlib, sasl]},
    {mod, { erlagi_app, []}},
    {env, []}
]}.
